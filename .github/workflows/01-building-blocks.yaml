name: 01-Building-Block
on: push

jobs:
    echo-hello:
        runs-on: ubuntu-latest
        steps:
            - name: Say HEllo
              run: echo "Hello, World"
    echo-goodbye:
        runs-on: ubuntu-latest
        steps:
            - name: Failed Steps
              run: |
                echo "I will fail"
                exit 1
            - name: Say-goodbye
              run: echo "goodbye!"
