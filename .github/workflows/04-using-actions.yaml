name: 04-Using Actions

on:
  workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        run: actions/checkout@v4
      - name: Printing Folders
        run: ls
